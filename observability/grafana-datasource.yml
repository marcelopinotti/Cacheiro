apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus   # fixo: o grafana-dashboard.json referencia este uid
    access: proxy
    url: http://prometheus:9090
    isDefault: true
